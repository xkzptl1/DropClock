# CLAUDE.md

このファイルは Claude Code がこのリポジトリで作業するためのガイドです。

## プロジェクト概要

**DropClock** — 透過ディスプレイ（主に透過OLED）向けの Windows 用「水時計」アプリ。

画面上端に並ぶ仮想水弁から放出された水糸・水セグメント・粒が、重力落下中の一瞬だけ `14:07` などの文字を形成し、そのまま崩れて落ちていく。通常の時計アプリではなく、**現実空間に光る水の糸と粒が浮かんで見えるデジタルインテリア**として設計する。

大阪駅・南ゲート広場の「水の時計」（株式会社光栄 SPACE PRINTER、384ノズル）の表現原理を参考にする。制御データや意匠を複製するのではなく、「落下する水を時間制御して一瞬だけ像を作る」という原理だけを借りる。

原典コンセプト: **[DropClock_Transparent_Display_Concept_v4.md](DropClock_Transparent_Display_Concept_v4.md)**（設計判断の一次ソース。実装方針で迷ったらここに戻る）

同じフォルダの `_Concept.md` / `_Concept_v2.md` は旧版。**v4 が正。** v1 は「1ドット = 1水滴」モデルだったが、v4 で水弁モデルに置き換わっている。

## 現在の状態

- **フェーズ: v4 コンセプトへ移行済み。水弁モデルが既定。v0.1 / v0.2 完了。v0.3 パターンライブラリに着手済み。**
- 柄は 6 つ（青海波 / 桜 / 梅 / ストライプ / カーテン / 音符と五線譜）。時刻と交互に挟むシーケンスで再生される。
- `.exe` 書き出し済み（`export/`）。自動起動も検証済み（現在は解除）。
- 描画モードは 2 つ。`segments`（v4 の水弁モデル・既定）と `drops`（旧 v0.1 の水滴モデル）。設定で切替可能。
- Godot 4.7.2 インストール済み。実行パスは下記「開発コマンド」参照。
- 空中で `HH:MM` が水滴として揃い、崩れて落ちることを実描画で確認済み。60fps 実測済み。
- 設定は `%APPDATA%/Godot/app_userdata/DropClock/config.cfg`。初回起動時に生成される。
- v0.2 で残っているのは実機依存の 1 つだけ: `luminous` / `lens` の最終選択（透過ディスプレイ上で決める）。
- 次は Priority B（四季の柄）と、抽象トランジションの残り。[TODO.md](TODO.md) 3.2 以降。
- 実装で当初計画から変えた点とその理由は [DONE.md](DONE.md) を参照。

## 技術構成

| 項目 | 選択 |
|---|---|
| エンジン | Godot 4.x |
| 言語 | GDScript（MVP）／必要になれば C#・GDExtension を検討 |
| 描画 | `GPUParticles2D` または MultiMesh + CanvasItem Shader |
| ターゲット | Windows 10/11、**1280×720（16:9）**、60fps |
| 仮想ノズル数 | **384**（実機準拠プロファイル。256/320/384 に変更可） |
| 出力 | ボーダーレス全画面 `.exe` |

## 想定ディレクトリ構成（実装時に作る）

```text
project.godot
scenes/          メインシーン・オーバーレイ設定UI
scripts/
  core/          時刻取得・スケジューラ・設定保存
  glyph/         文字→白黒画像→ドットマトリクス変換
  drop/          水滴シミュレーション・ノズル管理
  display/       モニター選択・ウィンドウ配置
shaders/         水滴シェーダー
assets/fonts/    ドットマトリクス化用フォント
export/          ビルド成果物（コミットしない）
```

## パターンライブラリ

柄は `assets/patterns/*.txt` のテキストファイル。`---` より上がメタデータ、下がマスク（`#` が点灯セル）。

```text
id: sakura
name: 桜
origin: osaka_water_clock_reference
season: spring
height: 0.50
top: 0.18
---
....####....
..########..
```

- **`origin` は必須の考え方**。実機由来（`osaka_water_clock_reference`）と DropClock 独自（`dropclock_original`）をデータ上で区別する（設計ルール 16）。後から柄を足す人がいても区別が残るのはファイルに書いてあるからで、コード側の分類ではない。
- `height` / `top` は画面に対する比率。省略すると設定値を使う。カーテンのように全高を使いたい柄だけ指定する。
- **`mode: scatter`** — ファイルを 1 つのモチーフとして扱い、レーン空間全体へ複数世代に分けて散らす。`generations` / `per_generation` / `stagger` / `lane_min` / `lane_max` / `band_rows` / `scale_min` / `scale_max` で制御。桜・梅がこれ。**装飾柄を画面中央の小さなアイコンにしないこと**（AGENTS.md 7 章、幅の 90〜96% を使う）。
- マスクは `GlyphBuilder.GlyphData` になる。**プランナから見ると文字列も柄も同じもの。**
- 幾何学的な柄（青海波・ストライプなど）は手で描かず生成した。生成スクリプトは残していないので、作り直すときは同じ方針で。

## ドメイン用語（コード内でもこの語を使う）

- **Nozzle / Valve（ノズル・水弁）** — 画面上端に等間隔で並ぶ仮想の放出点。既定 384 本。水の X 座標はノズル位置で決まる。
- **Segment / Water thread（水セグメント・水糸）** — **描画の基本単位。** あるノズルの弁が `OPEN` から `CLOSE` までの間に放出した、ひとつながりの水。短い開放なら粒、長ければ水糸、閉じなければカーテン。
- **Pulse（パルス）** — 1 回の `OPEN → CLOSE`。1 パルス = 1 セグメント。
- **Glyph（グリフ）** — 表示したい文字列を白黒化しドットマトリクス化したもの。
- **Cell（セル）** — グリフ内の 1 マス。**セルは水滴ではない。** 縦に連続したセルは 1 本のパルスへ統合される。
- **Formation time（形成時刻）** — 全ての水が空中で像を作る瞬間。この瞬間も水は静止しておらず落下し続けている。
- **Cycle（サイクル）** — 「弁開放 → 形成 → 崩壊 → 消滅 → 無表示」の一巡。

## 中核となる計算

### 落下モデル

```text
y(t) = v0 * t + 1/2 * g * t²
```

`v0`（初速）と `g` は現実の値ではなく**演出パラメータ**。逆関数（ある高さに届く時刻）は

```text
t = (-v0 + sqrt(v0² + 2*g*y)) / g
```

どちらも `DropSettings.fall_distance()` / `DropSettings.time_to_fall()` に実装済み。**プランナと描画側が別々に落下式を持たないこと。**

### セル → 水弁パルス（v4 の核心）

グリフの縦方向に連続したセルは、**独立した水滴にせず 1 本のパルスへ統合する。** これが粒・水糸・カーテンを単一の仕組みから出す唯一の方法。

```text
同じノズル上の連続6セル

  ●
  ●
  ●   →   VALVE OPEN ───────── VALVE CLOSE
  ●        （1本の水セグメント）
  ●
  ●
```

先に出た水ほど長く落ちている。したがって 1 パルスの中では、**最初に出た水がセグメントの下端**になる。

```text
open_time  = formation_time - time_to_fall(y_bottom)   ← 早い
close_time = formation_time - time_to_fall(y_top)      ← 遅い
```

描画は毎フレーム 2 点を求めて、その間を水として描く。

```text
y_front = fall(now - open_time)                  先端
y_back  = fall(max(now - close_time, 0))         後端
```

`now < close_time` の間は後端が画面上端に固定され、ノズルから伸び続けるカーテンになる。形成後は先端の方が速いのでセグメントが伸びていく。**この伸びが像の崩壊そのもの**で、崩壊のアニメーションは書かない。

**不変条件:** 1 つの弁が同時に二度開くことはない。同じノズル上のパルスは時間区間が重ならない。グリフの連続領域は互いに素なので自動的に満たされるが、セグメントごとに運動を変える変更（左右揺れ、分裂、ノズル別の重力）を入れる場合は `ValvePlanner.verify_no_overlap()` で必ず検証すること。

## 正準キャンバス 1280x720（AGENTS.md 3 章・最優先）

**シミュレーションは常に 1280x720 の固定座標系で行う。** ウィンドウサイズ・DPI・最大化/復元・モニター移動は、**最終的な表示変換にしか影響してはいけない。**

`project.godot` で担保している。

```text
window/stretch/mode="canvas_items"
window/stretch/aspect="keep"
```

これにより `get_viewport_rect().size` は常に 1280x720 を返し、Godot が一様スケール + レターボックスを適用する。仕様書の式と同じ。

```text
scale = min(viewport_w / 1280, viewport_h / 720)
```

**エミッタ間隔・柄の配置・重力・タイミングをウィンドウサイズから計算しないこと。** 検証は 4 種のウィンドウサイズ（16:9 と非 16:9 を含む）で計画値が完全一致することを確認する。

## 設計上の絶対ルール

これらはコンセプトの中核なので、明示的な指示なしに破らないこと。v4 の「アニメーション原則」を含む。

### 水の出し方

1. **水は画面上端の仮想ノズルからのみ放出する。** 途中から湧かせない。
2. **各ノズルは `OPEN` / `CLOSE` される。** 開いている時間が水の長さになる。
3. **表現単位を「丸い粒」に限定しない。** 粒・短い線分・長い水糸・連続カーテンを同じ仕組みで許容する。
4. **水は重力で下方向に加速し続ける。** 等速で落とさない。
5. **時計・文字・柄は落下中の水が一瞬だけ形成する。** 形成後も静止させない。
6. **完成画像を直接表示しない。** 図形全体を 1 枚の Sprite として落とさない。
7. **パターンは原則 `nozzle × time` の水弁制御データとして生成する。**

### 見せ方

8. **固定UIを出さない** — タイトルバー・枠・設定ボタン・ロゴ・ステータス表示すべて禁止。設定オーバーレイは Ctrl+Alt+D で呼ぶ時だけ。
9. **背景は完全な黒**（`#000000`）。透過OLEDでは黒 = 非発光 = 現実の背景が透ける。グレーの背景やグラデーションは透過を殺す。実機で最も透けて見える値へ調整可能にはする。
10. **マウスカーソルは非表示**（オーバーレイを開いている間を除く）。
11. **何も表示されない時間は演出の一部** — 常時表示にしない。無表示区間を短縮する方向の「改善」はしない。
12. **文字フォントを直接描画しない** — フォントはドットマトリクス生成の中間表現としてのみ使う。
13. **色は白〜青白**。彩度の高い青にしない（透過ディスプレイで不自然になる）。
14. **60fps を維持**。水の量が増える機能を足すときは実測する。

### 実機リファレンスの扱い

15. 大阪駅実機の制御データ・意匠データを複製しない。**表現原理だけを参考にする。**
16. 実機由来のパターンと DropClock 独自パターンは、データ上でも `origin` で区別する。

## 開発コマンド

winget で入れた場合の Godot 実行ファイル（環境変数で書く。**絶対パスをコミットしないこと**）:

```text
%LOCALAPPDATA%\Microsoft\WinGet\Packages\GodotEngine.GodotEngine_Microsoft.Winget.Source_8wekyb3d8bbwe\Godot_v4.7.2-stable_win64_console.exe
```

`_console.exe` のほうを使うこと。標準出力が取れる。以下は `godot` に通っている前提で書く。

エディタで開く:

```bash
godot --editor --path .
```

開発用（ウィンドウ 1280x720・カーソルあり・Esc で終了）:

```bash
godot --path . -- --dev
```

グリフのパターンだけを静止表示して読めるか確認する:

```bash
godot --path . -- --dev --glyph-test=14:07
```

接続モニターの一覧を出して終了:

```bash
godot --path . -- --list-screens
```

2 台目のモニターに全画面で出す（本番の起動形）:

```bash
godot --path . -- --screen=1
```

Windows 向けエクスポート:

```bash
godot --headless --path . --export-release "Windows Desktop" export/DropClock.exe
```

エクスポートテンプレート（4.7.2.stable / Windows x86_64）は導入済み。置き場所は `%APPDATA%/Godot/export_templates/4.7.2.stable/`。バージョンがずれるとエクスポートが失敗するので、Godot を上げたらテンプレートも入れ直すこと。

書き出されるのは 2 つ。`DropClock.exe`（本体）と `DropClock.console.exe`（標準出力が取れるラッパー）。**動作確認にはコンソール版を使う。**

### アプリの起動引数

| 引数 | 意味 |
|---|---|
| `--dev` | ウィンドウ 1280x720・カーソル表示・診断ログ出力 |
| `--screen=N` | 全画面表示するモニター番号（既定 0） |
| `--list-screens` | 接続モニターを表示して終了 |
| `--autostart=on\|off\|status` | Windows 自動起動の登録・解除・確認をして終了。**エクスポート版でのみ動作** |
| `--glyph-test=TEXT` | 時計を止め、TEXT を目標高さに静止表示 |
| `--cycle=SECONDS` | 表示サイクルの長さ（既定 30 秒） |
| `--dev=WxH` | 開発ウィンドウのサイズ（既定 1280x720） |
| `--mode=NAME` | 描画モード `segments`（既定・水糸）/ `drops`（水滴） |
| `--look=NAME` | 水滴の見た目 `luminous`（既定）/ `lens`。`drops` モードのみ |
| `--pattern=ID` | 柄を静止表示（`--glyph-test` の柄版） |
| `--list-patterns` | パターンライブラリを表示して終了 |
| `--live` | プレビューを静止させず動かす（柄は動きで判断する） |
| `--season=NAME` | 季節を強制 `spring` / `summer` / `autumn` / `winter` |
| `--capture=FILE` | PNG を保存して終了。見た目の確認用 |
| `--capture-after=SEC` | 撮影時刻（既定 1.0 秒） |
| `--capture-at-align` | 文字が揃った瞬間に撮影 |
| `--settings` | 起動時に設定オーバーレイを開く（撮影確認用） |
| `--seed=N` | サイズ・明るさのランダムを固定 |

`--` の後ろに書くこと。Godot 自身の引数と混ざらないようにするため。

- **Esc** — 設定オーバーレイが開いていれば閉じる。閉じていれば終了。
- **Ctrl+Alt+D** — 設定オーバーレイの開閉。
- コマンドライン引数は保存された設定を**その回だけ**上書きする。`user://config.cfg` には書き戻さない。

## 開発時の注意

- **`class_name` を持つスクリプトを新規追加したら、必ず一度 `--import` を走らせること。**

  ```bash
  godot --headless --path . --import
  ```

  グローバルクラス名は `.godot/global_script_class_cache.cfg` に登録される。更新せずにゲームを起動すると新しいクラスが解決できず、**ウィンドウが出たまま無反応になる**。エラーダイアログも出ないので原因が分かりにくい。一度これで詰まっている。

- **見た目を変えたら `--capture` で撮って確認する。** 20 秒サイクルを目視で待つ必要はない。

  ```bash
  godot --path . -- --dev=1920x1080 --capture=out.png --capture-at-align
  ```

- **透過ディスプレイは未確認** — メーカー・パネル方式（OLED/LCD）・解像度・接続方式が未確定。そのため「黒背景方式」と「ウィンドウ実透過方式」のどちらを最終採用するかは保留中。実装は黒背景方式を前提に進め、透過方式へ差し替え可能なようにレンダリング層を分離しておく。詳細は [PLAN.md](PLAN.md) の「未決事項」参照。
- 開発中は通常モニターの**ウィンドウモード**で作業する。全画面ボーダーレス + カーソル非表示のまま起動すると、デバッグ時に操作不能になりやすい。デバッグ用の脱出キー（Esc）を必ず用意する。
- パフォーマンス検証は「1920px 幅・160ノズル・グリフ最大ドット数」の最悪ケースで行う。
- 作業を終えたら [TODO.md](TODO.md) の該当項目を [DONE.md](DONE.md) へ移す。
