# TODO.md — DropClock

完了した項目は [DONE.md](DONE.md) へ移す。設計の根拠は [PLAN.md](PLAN.md)。

---

## 0. 事前確認（実装と並行可・ただし早めに）

透過ディスプレイの仕様が未確定。ここが埋まるまでは黒背景方式で進める。

- [ ] 透過ディスプレイのメーカー・製品名を確認
- [ ] 透過OLED か 透過LCD かを確認（**最優先**。LCD なら表示方式の再検討が必要）
- [x] ~~解像度を確認~~ → **1280x720 / 16:9 で確定**（v4）
- [ ] リフレッシュレートを確認
- [ ] 接続方式（HDMI / DP / USB-C）を確認
- [ ] Windows 上で通常の外部モニターとして認識されるか確認
- [ ] 上記を踏まえ「黒背景方式 / ウィンドウ実透過方式」を確定
- [ ] 実効輝度を見て水滴の明るさ上限を決める

---

## 1. 公開（handoff 要件）

- [x] AGENTS.md / docs/ をリポジトリへ取り込み
- [x] 正準 1280x720 キャンバス（AGENTS.md 3 章）
- [x] 桜を全幅・多世代の水シーケンスへ（docs/SAKURA_PATTERN.md）
- [x] README を現状に合わせて更新
- [x] マシン固有パス・秘密情報がコミットに含まれないことを確認
- [x] `.gitignore` にビルド成果物を追加
- [x] MIT ライセンスを追加
- [x] ローカルコミット
- [ ] **GitHub へ push（認証待ちでブロック中）** — `gh auth login` はブラウザ認証が必要なため代行できない
- [ ] Windows ビルドを GitHub Release として公開

---

## 2. 既知の問題

- [ ] **ワイド画面でグリフが小さくなる。** `glyph_height_ratio = 0.34` が横幅の制限より先に効く。**v4 でターゲットが 1280x720（16:9）に確定したため優先度は下がった**が、開発機の 3440x1440 で確認するときは小さく出る。16:9 では問題ない。
- [ ] **まれに 75-87ms のフレーム落ちが出る。** 柄を含むシーケンスを 50 秒回すと、リリースビルドで 2968 フレーム中 1 回、エディタのデバッグビルドでは 5 回。**画面に水が 0 本のときにも起きる**ので描画負荷ではない。デバッグビルドの方が明らかに多いことから、少なくとも一部はデバッグ実行の副作用。原因未特定。フィールドが空になる瞬間と相関しているように見えるが、確証はない。**このマシンでは裏で別処理も動かしていたので、まずは静かな状態で再測定すること。**
- [ ] **60fps の実測が vsync 律速。** 最悪フレームが常に 16.7ms（= vsync 1 コマ）で落ちゼロ。これは「60fps を維持できている」ことしか示さず、余力の大きさは分からない。上限を知りたければ vsync を切って測る必要がある。
- [x] ~~カーテン表現の負荷は未測定~~ → 実測した。336 本の全高セグメントで 60fps 維持。統合のおかげでセグメント数自体は小さい

---

## 3. v0.2 — 水らしさと実用性

### 3.1 ビジュアル  [完了 — DONE.md 2026-08-29 (3)]

以下は `drops` モードの水滴シェーダー。v4 の既定である `segments` モードは `shaders/water_thread.gdshader` を使う。

- [x] 水滴シェーダー: 少し縦長
- [x] 水滴シェーダー: 中央をやや透明に（`lens` プリセット）
- [x] 水滴シェーダー: 上部ハイライト
- [x] 水滴シェーダー: 落下方向の軽いモーションブラー
- [x] 水滴サイズのランダム化
- [x] 明るさのランダム変化
- [x] 色を白〜青白のレンジで調整（彩度の高い青にしない）
- [ ] **実機の透過ディスプレイで `luminous` / `lens` のどちらを既定にするか決める**（`--look=NAME` で切替）

### 3.2 表示内容  [完了]

- [x] 日付表示（`AUG 29`）
- [x] 曜日表示（`SATURDAY`）
- [x] 表示間隔のスケジュール調整（既定サイクル 30 秒。3 種を等間隔に配置すると 10 秒おきになり、コンセプトの例と一致する）

### 3.3 設定  [ほぼ完了]

- [x] `ConfigFile` による設定の保存・読込（`user://config.cfg`）。初回起動時にデフォルトを書き出す。読み込み時に全項目を clamp するので手編集で壊れない
- [x] `Ctrl + Alt + D` で設定オーバーレイを呼び出し（常設UIは作らない）
- [x] 設定項目: 水滴サイズ / 速度 / ノズル数 / 明るさ / 表示間隔
- [x] 設定項目: 日付ON・OFF / 曜日ON・OFF / FPS / 使用ディスプレイ / 見た目
- [x] 使用モニターを記憶して次回自動適用
- [x] Windows 起動時の自動起動（HKCU の Run キー、管理者権限不要）
- [x] **自動起動を実機で確認した。** `.exe` を書き出し、登録 → レジストリ確認 → Explorer と同じ形でコマンドを実行して起動確認 → 解除、まで通した。**現在は解除済み**（登録したままにはしていない）

---

## 4. v0.3 — 演出拡張とパターンライブラリ

v4 で追加された実機リファレンスに沿う。詳細は [コンセプト v4](DropClock_Transparent_Display_Concept_v4.md) の「大阪駅『水の時計』実機リファレンス」節。

### 4.1 パターンの仕組み  [完了]

- [x] パターンをデータとして定義する仕組み（`assets/patterns/*.txt` のテキストマスク）
- [x] パターンのメタデータに `origin` を持たせ、実機由来と独自を区別する
- [x] `Sequence`（Clock / Pattern / Weekday / Date / Pause を並べる）
- [x] 季節でパターンを選ぶ（`season:` タグ + 現在月から判定、`--season=` で強制可）
- [x] 一巡の長さを設定可能に（既定 90 秒 / 9 ステップ = 10 秒間隔。最大 900 秒まで）
- [ ] PNG シルエットからパターンを作る経路（今はテキストマスクのみ）
- [ ] `Transition` を独立した種類として扱う（今は普通のパターンとして混ぜているだけ）

### 4.2 Priority A — 実機の核

- [ ] `OSAKA` / `STATION` / `CITY` 型の 3 段文字シーケンス
- [x] 青海波（波形パターンの基準）
- [x] 桜
- [x] 梅
- [x] 音符 + 五線譜
- [x] ストライプ / カーテン
- [ ] Welcome 系メッセージ（多言語）

### 4.3 Priority B — 四季

- [ ] 春: 花・花びら
- [ ] 夏: ひまわり / 花火（**上から落ちる水のタイミング差で放射状を作る。下から打ち上げない**）
- [ ] 秋: 赤とんぼ / 紅葉 / 稲穂 / ぶどう / 案山子
- [ ] 冬: クリスマスツリー / スノーマン
- [ ] 通年: 松

### 4.4 Priority C — DropClock 独自

- [ ] 任意テキスト表示
- [ ] ユーザー画像から生成したシルエット
- [ ] 天気アイコン
- [ ] ピクセルアート / 任意の SVG・PNG

### 4.5 抽象・トランジション

実機は「意味のある絵柄」だけを出しているわけではない。時刻 → 柄 → 時刻と機械的に切り替えず、間に抽象的な水の動きを挟むことで**「時計アプリ」ではなく水のインスタレーション**として見せる。

- [x] カーテン状に流れる水 — `curtain` パターンとして実装。**47040 セルが 336 パルスに統合される（140 倍）**
- [ ] 水糸が 1 本ずつ消えていく演出
- [ ] 丸が重なった幾何学パターン
- [ ] 全面を使った水の面表現

### 4.6 水の質感

- [ ] ノズルごとの左右揺れ（**追い越し・弁の二重開放が起きないか要検証**）
- [ ] 一部セグメントの分裂
- [ ] 落下速度の微差
- [ ] 画面下端のスプラッシュ（本体の水糸とは別システム。ここだけ GPUParticles2D を使う）
- [ ] 屈折風シェーダー

### 4.7 モード

- [ ] Ambient Mode（`HELLO` / `DROP` / `:)`）
- [ ] Screensaver Mode: Windows アイドル検出 → 全画面 → 入力で解除

---

## 5. ビルド・配布

- [x] Windows エクスポートプリセットを設定（`export_presets.cfg`）
- [x] エクスポートテンプレート 4.7.2.stable を導入（Windows x86_64 のみ）
- [x] `.exe` を書き出して起動確認（104MB、pck 埋め込み、60fps）
- [ ] 別マシンで起動確認（このマシンでしか試していない。VC++ ランタイム等の依存は未確認）
- [ ] アイコンを用意する（現在は Godot の既定アイコン。`application/modify_resources` を有効にすると rcedit が要る）
- [ ] 実機の透過ディスプレイで表示確認・水糸の太さと明るさの最終調整
