# PLAN.md — DropClock 実装計画

コンセプト: **[DropClock_Transparent_Display_Concept_v4.md](DropClock_Transparent_Display_Concept_v4.md)**（v1 / v2 は旧版）
作業ルール: [CLAUDE.md](CLAUDE.md)

---

## 1. 全体アーキテクチャ

```text
┌─────────────────────────────────────────────┐
│ App (Main)                                  │
│  起動 → ディスプレイ選択 → 全画面 → ループ  │
└───────────────┬─────────────────────────────┘
                │
       ┌────────┴────────┐
       ▼                 ▼
┌──────────────┐  ┌────────────────┐
│ Scheduler    │  │ DisplayManager │
│ 何をいつ出すか│  │ モニター/窓/黒 │
└──────┬───────┘  └────────────────┘
       │ 表示要求 (text, align_time)
       ▼
┌──────────────┐
│ GlyphBuilder │  文字列 → 白黒画像 → ドット配列
└──────┬───────┘
       │ Dot[] (grid_x, grid_y)
       ▼
┌──────────────┐
│ DropPlanner  │  ドット → ノズル割当 + 発射時刻逆算
└──────┬───────┘
       │ DropSpec[] (nozzle_index, y_target, t_launch)
       ▼
┌──────────────┐
│ DropField    │  水滴の生成・落下・消滅（描画層）
└──────────────┘
```

各層は一方向にしか依存しない。`DropField` は「何の文字か」を知らない、`GlyphBuilder` は物理を知らない。

---

## 2. モジュール仕様

### 2.1 GlyphBuilder（文字 → ドット）  [実装済み]

責務: 表示したい文字列を、水滴 1 粒に対応するドット座標の配列へ変換する。

**当初案（TTF レンダリング → ダウンサンプル → 2値化）は採用しなかった。** 1 セル = 1 水滴という解像度では、TTF を縮小したときにどのセルが残るか予測できず、`8` と `9` のような字が潰れる。代わりに 5x7 のビットマップフォントを手で持ち、それを唯一のラスタ源とした（`scripts/glyph/font5x7.gd`、46 文字）。

```text
"14:07"
  ↓ 5x7 ビットマップフォントを引く
  ↓ dot_scale 倍に拡大（各フォントピクセル → scale x scale 個の水滴）
  ↓ ON セルを列挙
Dot[] = [(gx, gy), ...]
```

- `dot_scale` はフォントの解像度を上げずに水滴数だけを増やすための倍率。既定 2（1 ピクセル → 4 滴）。
- グリッド幅は文字数から決まる（1 文字 = 6 セル、うち 1 セルは字間）。`14:07` で 58 セル × 14 行。
- 任意文字（v0.3）で 5x7 に無い字が必要になったら、TTF ラスタライザを同じ `GlyphBuilder.build()` の裏に足す。呼び出し側は変わらない。
- 図形（雪・葉・傘など）は、同じ「テキスト行の配列」形式でパターンを直接書けば載る。Seasonal Mode はここに乗る。

### 2.1b ValvePlanner（セル → 水弁パルス）  [実装済み / v4 の核心]

責務: グリフのセルを、ノズルごとの `OPEN` / `CLOSE` 区間へ変換する。**v4 で追加され、既定の経路になった。**

**縦に連続したセルを 1 本のパルスへ統合する**のがこのモジュールの存在理由。統合しないと粒が縦に並ぶだけで、水糸にもカーテンにもならない。

```text
"14:07" → セル 5800 個 → 統合 → 水弁パルス 320 本（18.1 倍の圧縮）
```

- グリフの拡大率は「ノズルを飛ばす」のではなく「1 フォントピクセルを何セルに広げるか」で決める（`step` 固定 1、`scale` 可変）。こうしないとストロークがスカスカになり、統合できる縦連続が生まれない。1280px / 384 ノズルで `14:07` は `scale = 10`、290 x 70 セル。
- 1 パルスの中では**最初に出た水が下端**。`open_time` は下端から、`close_time` は上端から逆算する。
- **不変条件:** 1 つの弁は同時に二度開かない。同じノズル上のパルスは時間区間が重ならない。`verify_no_overlap()` で検証できる。

---

### 2.1c SegmentField（水弁パルスの描画）  [実装済み / v4]

毎フレーム、パルスごとに 2 点を求めてその間を描く。

```text
y_front = fall(now - open_time)
y_back  = fall(max(now - close_time, 0))
```

- `now < close_time` の間は後端が上端に固定される。**これがカーテン表現**で、別処理を書く必要がない。
- 形成後は先端の方が速いのでセグメントが伸びる。**この伸びが崩壊そのもの。** 崩壊アニメーションは存在しない。
- シェーダー（`shaders/water_thread.gdshader`）は縦カプセル 1 種類だけ。長いクオッドなら水糸、幅と同程度なら粒に見える。**モード分岐はない。**
- 水滴ごとの値は modulate 色で渡す。rgb = 色 x 明るさ、**a = キャップ半径（クオッド高さに対する比）**。

---

### 2.1d PatternLibrary（柄の読み込み）  [実装済み / v0.3]

`assets/patterns/*.txt` を読み、`GlyphBuilder.GlyphData` を返す。書式は [CLAUDE.md](CLAUDE.md) の「パターンライブラリ」節。

**マスクを共通通貨にしたのが要点。** 文字列も柄も同じ `GlyphData` になるので、プランナ側は「これが花なのか時刻なのか」を知らない。そのため柄を足してもプランナと描画には一切手が入らない。

- `origin` をファイルに持たせ、実機由来と独自を区別する（設計ルール 16）。コード側の分類にすると、柄を追加する人がそこを通らずに済んでしまう。
- `height` / `top` は柄ごとの footprint 上書き。カーテンだけ全高を使いたい、といった要求をプランナの引数で吸収する。
- 幾何学的な柄は手描きせず生成した。ASCII で見た目を判断するのは当てにならない（青海波は ASCII では失敗に見えたが、画面では正しく読めた）。**柄の可否は必ず `--pattern=ID` で描画して判断すること。**

---

### 2.1e MotifScatter（柄の散布）  [実装済み / v0.3]

1 つのモチーフを正準レーン空間へ多数配置し、**世代ごとにずらした形成時刻で放出する。**

- 配置は**層化 + ジッタ**。レーン範囲を motif 数で分割し、各帯の中でランダムに置く。完全ランダムだと固まって巨大な空白ができ、等間隔だと格子になる。仕様はそのどちらも明確に禁じている。
- 世代ごとに帯を位相シフトするので、世代同士が横方向に噛み合う。位相をかけないと全世代が同じ 7 箇所に重なり、密度が上がらずただ太るだけになる（実際に一度そうなった）。
- 各世代は**普通の Plan**。違うのは形成時刻だけ。新しい描画経路は要らない。水は相変わらず上端から出て落ちるだけ。
- 合成マスクは最初から 384 レーン幅なので、プランナには `fixed_scale = 1` を渡して再スケールを止める。

---

### 2.2 DropPlanner（ドット → 発射計画）  [実装済み / v0.1 の水滴モデル]

責務: ドット配列を、実際に落とす水滴の仕様へ変換する。

- `nozzle_index = round(dot_screen_x / nozzle_pitch)` でノズルへスナップ。X はノズル位置に丸められるので、グリッド幅とノズル数は整合させる。
- `y_target = glyph_top_y + gy * cell_height`
- `t_fall = sqrt(2 * y_target / g)`、`t_launch = t_align - t_fall`
- 出力は `t_launch` の昇順にソートしたキュー。

考慮点:

- 最上段のドットは `t_fall` がほぼ 0 → 整列時刻の直前に発射される。グリフ上端は画面上端から十分下げる（例: 画面高の 25〜40%）。そうしないと最上段の水滴が「湧き出た瞬間に文字」になり不自然。
- `g` は現実の重力ではなく演出パラメータ。**`g = 600 px/s²` に決定**（1920x1080・ノズル 160 本・`glyph_top_ratio = 0.25` 前提）。この値で:
  - 最初の水滴が出てから文字が揃うまで **1.42 秒**
  - 揃ってから最後の水滴が画面外へ出るまで **0.98 秒**
  - 1 サイクル約 **2.4 秒**。コンセプトの「:00 発射 → :01 整列 → :02 崩壊 → :03 消滅」とほぼ一致する。
- グリフを画面の下に置くほど整列後の落下距離が減り、崩壊の時間が短くなる。`glyph_top_ratio` を上げるときはこのトレードオフを意識する。

### 2.3 DropField（水滴の生成と描画）  [実装済み / v0.1 の水滴モデル]

責務: 発射キューを消化し、水滴を落とし、画面外で破棄する。

**Sprite2D プールではなく、単一 Node2D の `_draw()` + 並列 PackedFloat32Array で実装した。** ノード生成・解放が一切なく、プールのバグが入る余地がない。MultiMesh への移行コストは Sprite2D 版と変わらない。

- **位置は積分せず解析的に求める。** `y = nozzle_y + 0.5 * g * (t - t_launch)²` を毎フレーム計算するので、フレームレートが揺れても意図した高さから誤差が蓄積しない。速度を持たないので状態も減る。
- 未発射の水滴（`t < t_launch`）は計算も描画もしない。発射キューを別に持つ必要がない。
- 破棄は swap-remove で O(1)。
- 加算合成（`BLEND_MODE_ADD`）。水滴は物体ではなく光なので、重なった箇所は明るくなるのが正しい。
- ドット数が増えて 60fps を割ったら `MultiMeshInstance2D` へ、さらに必要なら `GPUParticles2D` + カスタムシェーダーへ移行する。**最初から GPU パーティクルに行かない**（発射時刻を個別指定する制御が書きにくく、デバッグが困難）。
- 見た目は v0.2 のシェーダーで作り込む。v0.1 は放射グラデーションの楕円のみ。
- **起動直後の 2 フレームだけ黒い矩形を 1 つ描く。** Vulkan がシェーダーのパイプラインを初回描画時にコンパイルするため、放置すると最初のグリフの途中で 90ms のストールが出る。加算合成では黒は何も足さないので、見た目を変えずにコンパイルを「何も表示されない時間」へ移せる。

### 2.3b DropLook（水滴の見た目）  [実装済み / v0.2]

`shaders/drop.gdshader` のユニフォームを 1 つの束にしたもの。プリセットを 2 つ持つ。

| プリセット | 性格 | 数値 |
|---|---|---|
| `luminous`（既定） | 内側まで柔らかく光る。水滴というより「光の粒」 | `hollow 0.42` / `rim_power 1.6` / `highlight 0.7` |
| `lens` | 上肩に鋭いハイライト、その下は暗い。透けて見える水のビーズ | `hollow 0.55` / `rim_power 1.2` / `highlight 0.95` |

**どちらを最終採用するかは保留。** 透過OLEDでは室内光と競合するため、モニター上で綺麗なほうが実機で良いとは限らない。実機が来てから決める。`--look=NAME` で切り替えられる。

水滴の形は UV だけから作る。`draw_texture_rect` が水滴ごとに渡せるのは modulate 色の 4 成分しかないので、rgb に色 x 明るさ、**a に正規化した落下速度**を載せている。速度は形（尾の伸び）と、CPU 側での矩形の縦伸ばしの両方を駆動する。MultiMesh へ移行すればインスタンスごとの custom data が使えるので、この制約は外れる。

**尾は上に伸ばす。** 進行方向と逆側、つまり水滴が通ってきた側に伸びるのが、露光中に水滴が占めた範囲として正しい。矩形の下端は物理が決めた位置に固定し、上へ伸ばしている。

### 2.4 Scheduler（演出スケジュール）  [実装済み]

責務: 「いつ何を出すか」の一元管理。

- 時刻ちょうど、または一定間隔で `HH:MM` を要求。
- 続けて `SATURDAY`、`AUG 29` を数十秒おきに要求（コンセプトの表示サイクル例）。
- **無表示区間を明示的な状態として持つ**。「暇だから何か出す」ロジックを入れない。
- 実装は「(表示時刻, 表示内容) のキュー」で十分。凝ったルールエンジンは要らない。

### 2.4b Sequence（演出の並び）  [実装済み / v0.3]

Scheduler が持つステップ列。実機の観察記録が「音符 → 時刻 → 時刻 → 時刻 → 桜」のように**時刻と柄を交互に挟む**形なので、それに倣う。

```text
Clock → Pattern → Clock → Weekday → Pattern → Clock → Date → Pattern → Pause
```

- 既定は 90 秒で 9 ステップ = 10 秒間隔。実機は約 5 分で一周するとされるので、`cycle_seconds` は 900 秒まで上げられる。
- **Pause は正式なステップ。** 割り算の余りではない。何も出さない時間は作品の一部なので、ここを埋める規則はどこにも書かない。
- 柄の候補は季節で絞る（`season:` タグ + 現在月）。プールは毎回シャッフルするので、長時間動かしても同じ組み合わせが続かない。

---

### 2.5 DisplayManager（ウィンドウ・モニター）  [実装済み]

責務: どのモニターに、どう出すか。

- 接続モニターの列挙と選択、選択結果の保存。
- ボーダーレス全画面、背景黒、カーソル非表示。
- デバッグ用にウィンドウモード起動オプションを持つ。

### 2.6 Config（設定）  [実装済み / v0.2]

- 保存先は `user://config.cfg`（Godot の `ConfigFile`）。**初回起動時にデフォルトを書き出す**ので、手編集できる実体が必ず存在する。読み込み時に全項目を clamp するため、手で壊れた値を書いても起動不能にはならない。
- **`AppConfig` は `DropSettings` とは別クラス。** DropSettings は設計上の定数（ノズル比、フィット上限、尾の形）も持つので、丸ごと永続化すると内部の調整ノブがそのままユーザー設定になってしまう。人が触る想定のものだけを AppConfig に置く。
- コマンドライン引数は設定をその回だけ上書きし、ファイルには書き戻さない。
- 項目: 水滴サイズ／速度／ノズル数／明るさ／表示間隔／日付ON・OFF／曜日ON・OFF／季節演出ON・OFF／FPS／使用ディスプレイ／自動起動。
- UI は `Ctrl + Alt + D` で呼び出すオーバーレイ。**常設UIは作らない**（v0.2 以降）。

---

### 2.6b 描画モードの二本立て

`DropSettings.render_mode` で選ぶ。設定オーバーレイからも切替でき、切り替えるとフィールドのノードごと差し替わる。

| モード | 単位 | 見え方 | 経路 |
|---|---|---|---|
| `segments`（既定） | 水弁パルス | 水糸・カーテン。実機に近い | ValvePlanner → SegmentField → water_thread.gdshader |
| `drops` | 1 セル 1 滴 | ドットマトリクス時計 | DropPlanner → DropField → drop.gdshader |

v4 は水弁モデルを core と定めているので既定は `segments`。ただし水滴モデルも残す。透過ディスプレイ上でどちらが良く見えるかは実機で決める話であり、モニター上の印象では判断できない。

---

### 2.7 実測済みのレイアウト結果

**以下は旧ターゲット（1920x1080・160 ノズル・水滴モード）の記録。** v4 で 1280x720・384 ノズルへ移行したため現行値ではない。移行後の実測は DONE.md 2026-08-29 (5) を参照。

1920x1080・ノズル 160 本（ピッチ 12px）・`dot_scale = 2` での計画結果。

| 文字列 | 水滴数 | セル | グリフサイズ |
|---|---:|---:|---|
| `14:07` | 232 | 24px | 1392 x 336 |
| `AUG 29` | 320 | 24px | 1680 x 336 |
| `SATURDAY` | 484 | 12px | 1128 x 168 |

`SATURDAY` は 8 文字あり 24px セルでは横幅に入らないため、自動で 12px セルへ落ちる。時刻より小さく表示されるが、時刻が主・曜日が従なので許容する。

理論上の最大同時水滴数は 160 ノズル x 14 行 = 2240。実際のグリフはその 1/5 以下。

---

## 3. マイルストーン

### v0.1 — MVP「時刻が水滴で現れて崩れる」

達成条件: 透過ディスプレイなしでも、黒背景のモニター上で `HH:MM` が水滴として一瞬揃い、崩れて消えるところまで。

- Godot 4 プロジェクト初期化
- ボーダーレス全画面・黒背景・カーソル非表示・Esc で終了
- マルチモニター選択（起動時に決め打ちでも可）
- 現在時刻取得
- `HH:MM` のドットマトリクス化
- 発射時刻逆算
- 水滴の落下と破棄
- 1920×1080 / 60fps

### v0.2 — 「水らしさ」と実用性  [実機確認待ちを除き完了]

- ~~水滴シェーダー（縦長・中央やや透明・上部ハイライト・下方向ブラー・サイズrandomize）~~ **完了**
- ~~日付表示 / 曜日表示~~ **完了**
- ~~水滴サイズ・落下速度の設定~~ **完了**
- ~~設定オーバーレイ（`Ctrl + Alt + D`）と設定の永続化~~ **完了**
- ~~使用モニターの記憶~~ **完了**
- ~~Windows 自動起動~~ **完了**

### v0.3 — 演出の拡張

- Ambient Mode（`HELLO` / `DROP` / `:)` など短い演出）
- Seasonal Mode（桜・傘と波・葉・雪をドットパターンで）
- 任意文字表示
- 画面下端のスプラッシュ
- スクリーンセーバー化（Windows アイドル検出 → 全画面 → 入力で解除）
- 演出スケジューラーの拡張

---

## 4. 未決事項（実装前に確定させたい）

コンセプト末尾の「次に確認したいこと」に対応。**ここが埋まるまでは黒背景方式を前提に実装する。**

- 透過ディスプレイのメーカー・製品名
- 透過OLED か 透過LCD か（LCD ならバックライト構造上、黒 = 非発光にならず、方式の見直しが必要）
- 解像度・リフレッシュレート
- 接続方式（HDMI / DisplayPort / USB-C）
- Windows 上で通常の外部モニターとして認識されるか

これが決まると次が決まる:

| 決めたいこと | 依存 |
|---|---|
| 黒背景方式 か ウィンドウ実透過方式 か | パネル方式 |
| HDR を使うか | パネル・接続方式 |
| 明るさ設計（水滴の輝度上限） | パネルの実効輝度 |
| 最適な水滴サイズ | 解像度・実寸サイズ |
| ノズル数 | 解像度 |

---

## 5. 技術的リスク

| リスク | 影響 | 対応 |
|---|---|---|
| 透過LCDだった場合、黒が「黒く光る」 | コンセプトの前提が崩れる | パネル方式の確認を最優先。LCD ならウィンドウ実透過方式へ切替 |
| 水滴数が多く 60fps を割る | 演出が破綻 | **実測済み: 1920x1080 で 60.x fps、最悪フレーム 16.7ms（= vsync 1 コマ、落ちなし）。** 現状は余裕あり。Sprite → MultiMesh → GPUParticles の移行パスも用意済み |
| ノズルへの X スナップで文字が読めない | 中核体験の劣化 | グリッド幅とノズル数を整合させる。文字が潰れるならノズル数を増やす |
| 自由落下だけだと動きが単調 | 「水らしさ」不足 | v0.2 で左右揺れ・分裂・速度微差を追加。ただし追い越し不変条件を壊さないこと |
| 全画面 + カーソル非表示でデバッグ不能 | 開発効率 | Esc 終了とウィンドウモード起動を v0.1 の最初に入れる |

---

## 6. 進め方

1. まず「1文字でいいので水滴が空中で揃う」ところを作る。ここが動けば構想全体が成立する。
2. 見た目の作り込み（シェーダー・スプラッシュ）は、揃う仕組みが動いてから。
3. モード追加（Ambient / Seasonal / Screensaver）は Scheduler と GlyphBuilder の拡張で吸収し、DropField には触らない。
