# DONE.md — DropClock 完了記録

[TODO.md](TODO.md) から完了した項目を、日付・内容・決まったことの順でここへ移す。

書式:

```text
## YYYY-MM-DD

- 完了した内容
  - 決まったこと / 実測値 / 後続への申し送り
```

---

## 2026-08-29

- コンセプト文書を作成
  - [DropClock_Transparent_Display_Concept.md](DropClock_Transparent_Display_Concept.md)
  - 透過ディスプレイ向け水時計として方向性を確定。「表示していない時間も演出の一部」を中核に置く。
  - 技術選定: Godot 4 + GDScript。理由は `.exe` 化の容易さ、2D描画とパーティクルの強さ、ボーダーレス全画面とマルチモニター制御のしやすさ。

- プロジェクト運用ドキュメントを整備
  - [CLAUDE.md](CLAUDE.md) — 作業ガイド。ドメイン用語、中核の逆算式、破ってはいけない設計ルール7項目を明文化。
  - [PLAN.md](PLAN.md) — アーキテクチャ（Scheduler / GlyphBuilder / DropPlanner / DropField / DisplayManager / Config の一方向依存）、マイルストーン、リスク。
  - [TODO.md](TODO.md) — v0.1〜v0.3 とビルドまでのタスク分解。
  - DONE.md（このファイル）

- 設計判断として記録しておくこと
  - 水滴の描画は Sprite2D → MultiMesh → GPUParticles の順に、必要になってから移行する。最初から GPU パーティクルには行かない（発射時刻の個別制御が書きにくくデバッグ困難なため）。
  - 同一ノズル内では下側のドットほど発射が早いため、水滴は追い越さない。衝突判定は不要。左右揺れや速度ランダム化を入れる際はこの不変条件を再検証する。
  - 全画面・カーソル非表示のままだとデバッグ時に操作不能になるため、Esc 終了とウィンドウモード起動を v0.1 の最初に実装する。

---

## 2026-08-29 (2) — v0.1 実装

**書いたコードはまだ一度も実行していない。** このマシンに Godot が入っていないため。検証は下記の代替手段で行った。

### 作ったもの

- `project.godot` / `.gitignore` / `scenes/main.tscn`
- `scripts/glyph/font5x7.gd` — 5x7 ビットマップフォント（0-9, A-Z, 記号 10 種、計 46 文字）
- `scripts/glyph/glyph_builder.gd` — 文字列 → グリッドセル
- `scripts/core/drop_settings.gd` — 調整パラメータ一式
- `scripts/drop/drop_planner.gd` — セル → ノズル割当 + 発射時刻の逆算、フィット計算、追い越し検証
- `scripts/drop/drop_field.gd` — 水滴の保持・落下・描画
- `scripts/display/display_manager.gd` — モニター選択・全画面・カーソル
- `scripts/core/scheduler.gd` — 表示サイクル
- `scripts/core/app.gd` — 起動・引数処理・Esc 終了

### 当初計画から変えた点

- **文字のラスタ化に TTF を使わない。** 1 セル = 1 水滴の解像度では、TTF を縮小したときどのセルが残るか予測できず字が潰れる。5x7 のビットマップフォントを手で持つ方式に変更。水滴数は `dot_scale`（既定 2、1 ピクセル → 4 滴）で稼ぐ。任意文字が要る v0.3 で TTF ラスタライザを同じ関数の裏に足せば済む。
- **Sprite2D プールをやめ、単一 Node2D の `_draw()` + 並列 PackedFloat32Array にした。** ノードの生成・解放が無く、プール由来のバグが入らない。MultiMesh への移行コストは変わらない。
- **水滴の位置を積分せず解析式で求める。** `y = nozzle_y + 0.5 * g * (t - t_launch)²`。フレームレートが揺れても意図した高さから誤差が蓄積しない。未発射の水滴は計算も描画もしないので、発射キューを別に持つ必要もない。
- 描画を加算合成にした。水滴は物体ではなく光なので、重なりは明るくなるのが正しい。

### 決めた数値

- `gravity = 600 px/s²`、`glyph_top_ratio = 0.25`、ノズル 160 本（ピッチ 12px）、`dot_scale = 2`
- この組み合わせで 1 サイクル約 2.4 秒（発射 → 整列 1.42s → 消滅 +0.98s）。コンセプトの ":00 発射 / :01 整列 / :02 崩壊 / :03 消滅" とほぼ一致する。
- `14:07` = 232 滴 / 24px セル / 1392x336。実測値は [PLAN.md](PLAN.md) 2.7 の表。

### 検証したこと（Godot 無しで可能な範囲）

- フォントデータ全 46 文字が 7 行 x 5 桁で、想定外の文字を含まないことをスクリプトで検査 — 問題なし
- `14:07` / `SATURDAY` / `AUG 29` をテキストとして描き出し、読めることを確認
- プランナのアルゴリズムを Python に移植し、7 種の文字列でフィット・水滴数・タイミング・画面内に収まるかを計算 — 全て想定どおり
- **追い越し不変条件**を同じ移植版で検査 — 全文字列で違反 0 件
- 全 `.gd` ファイルの括弧balance とインデント（タブ統一）を検査 — 問題なし

これらは構文チェックの代わりにはならない。GDScript のパースが通るかは Godot で実行するまで不明。

### 次にやること

[TODO.md](TODO.md) 1.7。Godot 4 を入れて `--dev` で起動し、まず動くことを確認する。

---

## 2026-08-29 (3) — Godot 導入、v0.1 完了、v0.2 水滴シェーダー

### Godot を入れた

winget で **Godot 4.7.2 stable**（公式 GitHub リリース、ハッシュ検証済み）。実行パスは [CLAUDE.md](CLAUDE.md) の「開発コマンド」に記載。`_console.exe` のほうが標準出力を取れる。

### v0.1 完了 — 実際に動いた

`14:07` が空中で水滴として揃い、崩れて落ちることを実描画で確認。**v0.1 の合格基準を満たした。**

- パースエラー・実行時エラーなし。8 クラスすべて登録された。
- 計画値と実行値が一致: 232 滴 / 58x14 セル / 24.0px / lead 1.42s（Python 移植版の予測と同じ）
- **60fps 実測**: 1920x1080 で 60.0 fps、最悪フレーム 16.7ms（vsync 1 コマ = 落ちゼロ）。1 秒サイクルでグリフを重ねた負荷試験で 1472 フレーム・ピーク 732 滴でも同じ。
- ボーダーレス全画面・カーソル非表示の本番起動パスを screen=1（3440x1440）で確認。60.3 fps。

### v0.2 水滴シェーダー

`shaders/drop.gdshader`。UV だけから水滴の形を作る。

- 涙型（上が細く尖り、下が丸い）、上肩にハイライト、中央は暗め、落下軸方向のモーションブラー。
- 水滴ごとの値は modulate 色に載せる（rgb = 色 x 明るさ、a = 正規化した落下速度）。`draw_texture_rect` がこれしか渡せないため。
- 矩形の下端を物理の位置に固定し、速度に応じて上へ伸ばす。尾は水滴が通ってきた側に伸びる。

**作る過程で 1 度作り直した。** 最初の版は `rim` の明るさを「局所的な幅で正規化した距離」から求めていた。すると細い尾が「全部が縁」と判定されて水滴で一番明るくなり、上向きの光る棘のように見えた。**絶対距離**から求めるように変えて解決。この手の間違いは数式を読んでも気付けず、拡大画像を見て初めて分かった。

### 見た目を 2 つ持つことにした

`scripts/core/drop_look.gd`。ユーザー判断により**どちらか一方に決めず両方残す**。

| プリセット | 性格 |
|---|---|
| `luminous`（既定） | 内側まで柔らかく光る。光の粒 |
| `lens` | 上肩に鋭いハイライト、下は暗い。透ける水のビーズ |

透過OLEDでは室内光と競合するため、モニター上の見え方で決められない。実機が来てから `--look=NAME` で見比べて決める。

### 開発用に足したもの

見た目を変えるたびに 20 秒サイクルを目視で待つのは無理なので、確認手段を先に作った。

- `--capture=FILE` / `--capture-after=SEC` / `--capture-at-align` — PNG を保存して終了
- `--dev=WxH` — 開発ウィンドウのサイズ
- `--look=NAME` — プリセット切替
- `--seed=N` — サイズ・明るさのランダムを固定。2 枚の差分が「変更した所」だけになる
- dev モード終了時に fps 平均・最悪フレーム・ピーク水滴数を出力

### 詰まった点（次回のため）

**`class_name` 付きスクリプトを新規追加したら `--import` を走らせること。** `drop_look.gd` を足してそのまま起動したら、`DropLook` が解決できずウィンドウが出たまま無反応になった。エラーダイアログも標準出力も何も出ない。グローバルクラス名は `.godot/global_script_class_cache.cfg` に登録されるため。[CLAUDE.md](CLAUDE.md) の「開発時の注意」に追記済み。

### 見つけた問題（未修正）

**ワイド画面でグリフが小さい。** 3440x1440 では横幅の 36% しか使わない（1920x1080 では 72%）。`glyph_height_ratio = 0.34` が横幅の制限より先に効くため。1920x1080 が対象なら問題ないが、透過ディスプレイのアスペクト比次第で直す必要がある。単に上限を上げるとグリフが下がって崩壊時間が減るので、フィット計算ごと見直すことになる。[TODO.md](TODO.md) 1 に記載。

---

## 2026-08-29 (4) — v0.2 残り（日付・曜日・設定・自動起動）

### 表示内容

`SATURDAY` と `AUG 29` を有効化。既定サイクルを 20 秒から **30 秒**へ変更した。3 種を等間隔に置くと 10 秒おきになり、コンセプト（:00 時刻 / :10 曜日 / :20 日付）とちょうど一致する。

`SATURDAY` は 8 文字あり 24px セルでは横幅に入らないので自動的に 12px セルへ落ちる（484 滴）。時刻の半分の高さになるが、時刻が主・曜日が従なので許容する。

### 設定

- `scripts/core/app_config.gd` — `user://config.cfg`。**初回起動時にデフォルトを書き出す**ので、手編集できる実体が必ず存在する。読み込み時に全項目を clamp するため、手で壊れた値を書いても起動しなくなることはない。
- **DropSettings とは別のクラスにした。** DropSettings は設計上の定数（ノズル比、フィット上限、尾の形）も持つ。同じものを永続化すると、内部の調整ノブを増やしただけで「ユーザー設定」が勝手に増えてしまう。人が触る想定のものだけを AppConfig に置く。
- コマンドライン引数は設定を**その回だけ**上書きし、ファイルには書き戻さない。端末から実験しても設定が壊れない。

### 設定オーバーレイ

`scripts/ui/settings_overlay.gd`。Ctrl+Alt+D で開閉、Esc でも閉じる（開いていない時の Esc は従来どおり終了）。開いている間だけカーソルを出す。11 項目。

**作る過程で 1 度作り直した。** 最初の版はコントロールをグリッドの添字（1, 4, 7...）で引いていた。行の形が違うものを 1 つ混ぜた瞬間に**エラーも警告もなく**別のコントロールを掴む。実際、スライダーの数値ラベルが全部空のまま出た。名前付きの変数で保持する形に変えた。数値ラベルもシグナル任せをやめ、同期時に直接書くようにした（値がたまたま一致していると `value_changed` が飛ばないため）。

### 自動起動

`scripts/core/autostart.gd`。HKCU の Run キーに登録する。**管理者権限が要らず、このアカウントにしか影響しない**ため、HKLM ではなく HKCU を選んだ。

**未検証。** エディタ実行では Godot 本体のパスを登録してしまうので `is_supported()` が false を返す。キー名と値の形式（`"C:\path	o.exe"`）が既存アプリと同じであることは読み取り専用の `reg query` で確認したが、実際の書き込みは `.exe` を書き出してから。レジストリには何も書いていない。

### 60fps を割っていたのを直した

計測に**遅いフレームの発生時刻を出す**機能を足したところ、**t=1.55s に 1 回だけ 90.9ms** のストールが出ていた。最初のグリフの真ん中。以降 20 秒・8 グリフ・最大 320 滴では一度も起きない。

原因は **Vulkan が水滴シェーダーのパイプラインを初回描画時にコンパイルしていた**こと。水滴が出る瞬間にちょうど当たっていた。

対策: 起動直後の 2 フレームだけ、黒い矩形を 1 つ描いてシェーダーを走らせておく。加算合成では黒は何も足さないので見た目に影響しない。コンパイルのコストが「何も表示されない時間」に移る。

結果: **2073 フレームで最悪 16.7ms（vsync 1 コマ）、遅延フレーム 0。**

### 検証したこと

- 設定ファイルの往復: 削除 → 起動でデフォルト生成 → 手編集した値（gravity 1000 / nozzle 80 / look lens / cycle 9）で起動 → ログの lead・セル数・look がすべて一致
- 表示ローテーション: 時刻 → SATURDAY → AUG 29 → 時刻 が繰り返されることをログで確認
- オーバーレイの描画を撮影して確認。中央寄せと数値ラベルの表示を修正済み
- 60.0 fps / 最悪 16.7ms / 2073 フレーム / ピーク 484 滴

---

## 2026-08-29 (5) — コンセプト v4 への移行（水弁モデル）

コンセプトが v4 に更新された。**表現の中核モデルが変わったので、描画経路を作り直した。**

### 何が変わったか

| | v1（旧） | v4（現行） |
|---|---|---|
| 描画の基本単位 | 丸い水滴 1 個 | **水弁の 1 パルスが作る水セグメント** |
| 縦に連続した 6 セル | 独立した 6 滴 | **1 本の水糸** |
| ターゲット解像度 | 1920x1080 | **1280x720 / 16:9** |
| ノズル数 | 160 | **384**（実機準拠） |
| 落下式 | `y = g t²/2` | `y = v0 t + g t²/2`（初速を追加） |

### 作ったもの

- `scripts/drop/valve_planner.gd` — セル → 水弁パルス。**縦連続セルの統合**がこのモジュールの存在理由
- `scripts/drop/segment_field.gd` — パルスの保持と描画
- `shaders/water_thread.gdshader` — 縦カプセル 1 種類
- `DropSettings.fall_distance()` / `time_to_fall()` — 落下式を 1 か所に集約。プランナと描画が別々に式を持たないようにした

### 統合の効果

```text
"14:07" → 5800 セル → 320 パルス（18.1 倍の圧縮）
```

セルをそのまま水滴にすると 5800 個になるが、水弁モデルでは 320 本で済む。**しかも見た目は「水滴の羅列」ではなく「水糸」になる。** 統合しないと粒が縦に並ぶだけで、水糸にもカーテンにもならない。

### 設計上の判断

- **グリフの拡大は「ノズルを飛ばす」のではなく「1 フォントピクセルを何セルに広げるか」で行う。** 前者だとストロークがスカスカになり、統合できる縦連続が生まれない。1280px / 384 ノズルで `14:07` は `scale = 10`、290 x 70 セル、ピッチ 3.33px。
- **カーテンは特別扱いしない。** `now < close_time` の間は後端を画面上端に固定するだけで、弁が開いたままの連続水流になる。
- **崩壊アニメーションは書かない。** 形成後は先端の方が速いのでセグメントが自然に伸びる。それが崩壊そのもの。
- シェーダーは 1 種類だけ。長いクオッドなら水糸、幅と同程度なら粒に見える。**粒・水糸・カーテンの分岐が存在しない**のが v4 モデルの利点。

### 水滴モードは残した

ユーザーの指示により、**旧 v0.1 の水滴モデルを設定項目として残した。** `DropSettings.render_mode` が `segments`（既定）/ `drops` を切り替え、設定オーバーレイの「描画モード」からも変更できる。切り替えるとフィールドのノードごと差し替わる。

どちらが良いかは透過ディスプレイ上で決める話で、通常モニターの印象では判断できない。`--mode=drops` でも試せる。

### 不変条件の置き換え

旧: 「同じノズル内で水滴が追い越さない」
新: **「1 つの弁が同時に二度開かない」**（同じノズル上のパルスは時間区間が重ならない）

グリフの連続領域は互いに素なので自動的に満たされる。`ValvePlanner.verify_no_overlap()` で検証できる。左右揺れや分裂を入れるときは要再検証。

### 設定に追加した項目

- 描画モード（水糸 / 水滴）
- 水糸の太さ（ノズルピッチに対する倍率、既定 1.15）
- 最短セグメント長（既定 5px。単一セルのパルスが 1px 未満にならないように）

ノズル数スライダーの上限を 320 → 512 に引き上げた（384 を選べるようにするため）。

### 数値の再調整

720p では落下距離が 1080p の 2/3 になるので `gravity` を 600 → **430** に。結果として形成まで 1.41 秒、消滅まで +0.95 秒となり、1080p 時代（1.42 / +0.98）とほぼ同じ体感になった。`nozzle_y` -24 → -16、`kill_margin` 60 → 40 も同じ比率で調整。

---

## 2026-08-29 (6) — エクスポートと自動起動の検証

### エクスポート

- エクスポートテンプレート 4.7.2.stable を導入（公式 GitHub リリースの `.tpz` 1222MB）。**Windows x86_64 の 4 ファイルと version.txt だけを取り出した**ので、展開後は約 200MB。全プラットフォーム展開すると 4GB 近くなる。
- `export_presets.cfg` を追加。pck 埋め込み、コンソールラッパー有効、`modify_resources=false`（アイコン差し替えには rcedit が要るため今は無効）。
- `export/DropClock.exe` 104MB を書き出し。エディタ無しで起動し、設定を読み、水弁モデルを 60.4fps で描画することを確認。

### 自動起動 — 検証して、バグを 1 つ直した

`.exe` ができたので初めて実行できた。スクリプトから叩けるように `--autostart=on|off|status` を追加してある。

**最初の実装は動いたが、レジストリに入った値が壊れていた。**

```text
実際に入った値:  C:/Users/<user>/.../DropClock.exe
入るべき値:     "C:\Users\<user>\...\DropClock.exe"
```

問題は 2 つ。

1. **区切りがスラッシュ。** `OS.get_executable_path()` はスラッシュを返す。
2. **引用符が消えていた。** `reg add /d` に渡した値は C ランタイムのコマンドライン解析を通るため、囲みの引用符が食われる。

1 は無害だが、2 は**パスに空白が入った瞬間に壊れる**。`C:\Program Files\...` に置いたら Windows はどこまでが実行ファイル名か判断できず、起動時に黙って失敗する。今回のパスにたまたま空白が無かったので動いていただけ。

対策として `reg add` をやめ、**`.reg` ファイルを書いて `reg import` する**方式に変更した。`.reg` なら格納されるバイト列を厳密に指定できる。ファイルは UTF-16LE + BOM で手組みする（Godot の FileAccess は UTF-8 しか書けないため）。非ASCIIのユーザー名でも壊れない。

修正後は Steam や Adobe が入れている値と同じ形式（引用符つき・円記号区切り）になった。

### 通した検証

1. 登録前 — エントリ無し
2. `--autostart=on` → レジストリにエントリ出現、値の形式を確認
3. **登録されたコマンド文字列をそのまま実行**（Explorer が起動時にやることと同じ）→ DropClock が起動し応答することを確認 → 終了
4. `--autostart=off` → エントリ消滅

**現在は解除済み。** 開発中のビルドを指す起動エントリを勝手に残さない。有効にするには `DropClock.exe --autostart=on` か、設定オーバーレイの「Windows 起動時に開始」から。

### 残り

- 別マシンでの起動確認（このマシンでしか試していない）
- アイコン（今は Godot の既定アイコンのまま）

---

## 2026-08-29 (7) — v0.3 パターンライブラリ（着手）

### まず仕組みから

柄を 1 つ足すたびにプランナや描画に手が入るようでは続かないので、先に**マスクを共通通貨にする**リファクタを入れた。

- `GlyphBuilder.scale_mask()` を追加し、`ValvePlanner.plan_mask()` / `DropPlanner.plan_mask()` を新設。
- 文字列も柄も同じ `GlyphData` になる。**プランナは「これが花なのか時刻なのか」を知らない。**
- 結果として、柄を追加してもプランナ・描画・シェーダーには一切手が入らない。

### パターン形式

`assets/patterns/*.txt`。`---` の上がメタデータ、下がマスク（`#` が点灯セル）。

- **`origin` をファイルに持たせた。** 実機由来（`osaka_water_clock_reference`）と独自（`dropclock_original`）の区別は設計ルール 16。コード側の分類にすると、柄を追加する人がそこを通らずに済んでしまうので、ファイルに書く形にした。
- `height` / `top` で柄ごとに画面上の footprint を上書きできる。カーテンだけ全高を使いたい、という要求をこれで吸収する。

### 作った柄（Priority A のうち 6 つ）

青海波 / 桜 / 梅 / ストライプ / カーテン / 音符と五線譜。すべて `origin: osaka_water_clock_reference`。

**幾何学的な柄は手で描かず生成した。** 手描きだと青海波のような規則的な柄は破綻する。

### 統合率が柄によって大きく違う — これが v4 モデルの狙いどおり

| 柄 | セル | パルス | 圧縮 | 見え方 |
|---|---:|---:|---:|---|
| カーテン | 47040 | 336 | **140x** | ノズル 1 本につき全高の水糸 1 本 |
| ストライプ | 5120 | 64 | **80x** | 縦棒 → 長い水糸 |
| 桜 | 4896 | 120 | **40.8x** | 塊 → 長い縦連続 |
| 青海波 | 7344 | 1104 | **6.7x** | 細い弧 → 短い粒 |
| 音符 | 5664 | 1076 | **5.3x** | 横線の五線譜 → 粒の列 |

**縦に伸びた形は水糸に、横に伸びた形や細い線は粒になる。** コンセプトが言う「粒、短い棒、水糸、カーテンが混在する見え方」が、柄ごとの分岐なしに出ている。

### シーケンス

Scheduler をステップ列に作り替えた。実機の観察記録が時刻と柄を交互に挟む形なので、それに倣う。

```text
Clock → Pattern → Clock → Weekday → Pattern → Clock → Date → Pattern → Pause
```

既定 90 秒 / 9 ステップ = 10 秒間隔。実機は約 5 分で一周するとされるので `cycle_seconds` は 900 秒まで上げられる。**Pause は正式なステップ**で、割り算の余りではない。

柄は季節で絞る（`season:` タグ + 現在月、`--season=` で強制可）。プールは毎回シャッフルする。

### ASCII で柄の可否を判断してはいけない

青海波は ASCII 出力では完全に失敗に見えたので 2 回作り直したが、**実際に描画したら正しく読めた**。セルの粗さと画面上の見え方は別物だった。以後、柄の判断は必ず `--pattern=ID` で描画して行う。CLAUDE.md にも書いた。

### 計測

50 秒のシーケンス（時刻 3 回 + 柄 3 種 + 曜日 + 日付 + 無表示）を回した結果。

| | 平均 fps | 最悪フレーム | 遅延フレーム | ピーク |
|---|---:|---:|---:|---:|
| リリースビルド（`.exe`） | 59.9 | 81.3ms | 2968 中 **1 回** | 1076 パルス |
| デバッグビルド（エディタ） | 59.8 | 86.8ms | 2961 中 **5 回** | 1076 パルス |

**カーテンの負荷は問題なかった。** 336 本の全高セグメントでも落ちない。統合のおかげでセグメント数自体が小さいため。

ただし**まれなフレーム落ちが残っている**。水が 0 本のときにも起きるので描画負荷ではない。デバッグビルドで明らかに多いので少なくとも一部はデバッグ実行の副作用だが、リリースでも 1 回出た。原因は未特定。計測中このマシンでは裏で別処理も動いていたため、静かな状態での再測定が先。[TODO.md](TODO.md) 1 に記載。

### エクスポート

`include_filter="assets/patterns/*.txt"` を追加し、柄がパックに入ることを確認した（`.txt` はリソースではないので、これを書かないと**エクスポート版だけ柄が消える**）。書き出した `.exe` から 6 つとも読めることを確認済み。

### 見つけて直したバグ

Scheduler のステップ種別を `"clock"` にしたのに、`_text_for()` は旧来の `"time"` を照合したままだった。**時刻ステップが黙って空文字を返し、時計が一度も表示されなかった。** エラーも警告も出ない。定数（`Scheduler.KIND_CLOCK` 等）を共有する形に変え、未知の種別には警告を出すようにした。

---

## 2026-08-29 (8) — handoff 仕様への対応（正準キャンバス + 桜シャワー）

`DropClock_handoff/` を受領。AGENTS.md と docs/ を仕様として取り込み、2 つの重大な不適合を直した。

### 1. 正準キャンバス（AGENTS.md 3 章の MUST NOT 違反）

**レイアウトをライブのビューポートサイズから計算していた**ため、ウィンドウを変えると `pitch = viewport.x / 384` が変わり、エミッタ間隔も柄の配置も変わっていた。仕様は「ウィンドウサイズはシミュレーション形状を変えてはならない」と明記している。

`project.godot` に以下を追加して解決。

```text
window/stretch/mode="canvas_items"
window/stretch/aspect="keep"
```

ビューポートは常に 1280x720 を返し、Godot が一様スケール + レターボックスをかける。仕様の式そのもの。

検証: 1280x720 / 1920x1080 / 1600x1000 / 1000x900 の 4 種で**計画値が完全一致**（320 パルス、290x70 セル、scale 10、lead 1.41s）。非 16:9 は正しくレターボックスされた（1000x900 → 1000x562）。

### 2. 桜（当該タスク）

旧実装は**中央に 1 輪**。仕様が名指しで禁じている失敗例そのものだった。

`scripts/glyph/motif_scatter.gd` を新設し、パターンに `mode: scatter` を追加。

- 配置は**層化 + ジッタ**。完全ランダムだと固まり、等間隔だと格子になる。仕様はどちらも禁止している。
- **世代ごとに帯を位相シフト**する。最初これを入れ忘れ、3 世代が同じ 7 箇所に重なって「太った 7 輪」になった。位相を入れて初めて噛み合った。
- 各世代は普通の Plan で、違うのは形成時刻だけ（0.20 秒ずらし）。新しい描画経路は不要。**水は相変わらず上端から出て落ちるだけ**で、スプライトは一切使っていない。

実測: 3 世代 x 9 モチーフ = 27 輪、772 パルス。**幅カバー率 93.0%**（仕様 90〜96%）、うち 98.9% が実際に点灯（大穴なし）。

梅も同じ理由で scatter 化した（AGENTS.md 7 章は装飾柄全般に及ぶ）。

### 検証（IMPLEMENTATION_TASK.md のチェックリスト）

| 項目 | 結果 |
|---|---|
| 1280x720 で実行 | OK |
| 桜の密度 | 幅 93.0%、27 輪、世代重なりあり |
| リサイズ | 計画値・正規化位置とも一致 |
| 最大化/復元 | 同一コード経路。より大きい/小さい/非 16:9 の 3 種で確認 |
| モチーフ関係の不変 | 正規化座標の差 最大 0.012（1 フレーム分の落下差） |
| 形成中も重力落下 | 解析式。静止は存在しない |
| 完成スプライト不使用 | マスク → 水弁パルスのみ |
| 他パターンの回帰 | 青海波 1104 / 音符 1076 / ストライプ 64 / カーテン 336 = 変更前と完全一致 |
| ドキュメント更新 | 実施 |

### 途中で踏んだもの

- `_field` を `Node2D` 型にしていたため `_field.now()` の戻り値型が推論できず**パースエラー**。エラーダイアログは出ず、ウィンドウが開いたまま無反応になる。明示的に `: float` を付けて解決。
- `--live` を足したとき、「プレビュー中か」と「静止描画か」を同じ関数で判定していたため、`--live` を付けるとプレビュー自体が無効になりスケジューラが動いて**時計が写っていた**。`_has_preview()` と `_is_static_preview()` に分離。

### 装飾柄の幅（AGENTS.md 7 章）も直した

桜だけ見ていて見落としていたが、**7 章は装飾柄全般に効く**。測ってみると 3 つ違反していた。

| 柄 | 修正前 | 修正後 |
|---|---:|---:|
| 青海波 | 50.0% | 89.8% |
| ストライプ | 50.0% | 89.6% |
| 音符 | 58.3% | 89.6% |
| カーテン | 87.5% | 89.3% |

原因はマスクが縦長すぎたこと。`scale` は幅と高さの小さい方で決まるので、高さ側が先に効いて横に伸びなかった。**マスクを横に長く作り直して解決**（青海波 64→115 列、ストライプ/音符 →86 列）。タイル柄なので端が切れても問題ない。

### 公開準備

- `AGENTS.md` と `docs/` をリポジトリへ取り込み
- `README.md` を現状に合わせて英語で書き直し
- **ドキュメント内のマシン固有パスを除去**（`C:\Users\<user>\...` → 環境変数 / プレースホルダ）。GITHUB_RELEASE.md が「ローカルパスをコミットしない」と明記しているため
- `.gitignore` を handoff 版ベースに統合し、`export/` と `*.exe` を追加（104MB の実行ファイルが入るのを防ぐ）

---

## 未完了のまま残っている重要事項

- 透過ディスプレイの実機仕様が未確認（特に **透過OLED / 透過LCD の別**）。解像度は v4 で 1280x720 に確定した。これにより「黒背景方式」か「ウィンドウ実透過方式」かが変わる。詳細は [TODO.md](TODO.md) の「0. 事前確認」。
